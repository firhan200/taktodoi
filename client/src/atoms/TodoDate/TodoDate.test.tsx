import { render, screen } from '@testing-library/react'
import TodoDate from './TodoDate'
import '@testing-library/jest-dom'

// describe("TodoDate", () => {
// 	it("should render date when it given", () => {
// 		render(<TodoDate date='2 minutes ago'/>)

// 		const dateElement = screen.getByRole("todo-date")

// 		expect(dateElement).toHaveTextContent("2 minutes ago")
// 	})
// })
